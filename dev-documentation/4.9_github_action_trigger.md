Implemented the orchestration refactor so cloud runs are external-triggered, while local Docker + Airflow flow remains untouched.

### What I changed

- Added a new secure backend endpoint at `POST /run-pipeline` in `src/backend/routers/pipeline_trigger.py`
  - Auth via `x-api-key` header
  - Compares against `API_SECRET_KEY`
  - Returns `403` for invalid key
  - Returns `409` if a run is already in progress (in-memory lock)
  - Calls `jobs.weekly_ingestion.run_pipeline()`
  - Returns `{ "status": "success" }` on success

- Wired this route into FastAPI in `src/backend/main.py`
  - Included new router
  - Added OpenAPI tag metadata for `pipeline`

- Refactored ingestion entrypoint in `src/jobs/weekly_ingestion.py`
  - Added stateless `run_pipeline()` as main callable
  - Kept `run_weekly_ingestion()` as backward-compatible alias (no scheduling behavior)
  - Added lifecycle logging:
    - start time
    - end time
    - duration
    - success/failure summary

- Archived internal scheduler in `src/scraper/scheduler.py`
  - Removed all looping/scheduling behavior
  - File now clearly documents it is disabled and external trigger architecture is required
  - Running it now logs a no-op archival message only

- Added env var docs in `.env.example`
  - Added `API_SECRET_KEY=...`
  - Added production reference note for Render

- Added GitHub Actions scheduled trigger workflow at `.github/workflows/trigger-weekly-pipeline.yml`
  - Weekly cron (Monday 01:00 UTC / 09:00 MYT)
  - Calls backend `POST /run-pipeline` with `x-api-key`
  - Also supports manual `workflow_dispatch`

### Validation run

- Backend import check passed: `python -c "import main; print('backend import ok')"` from `src/backend`
- Python syntax check passed via `python -m compileall` on touched Python files
- Lint diagnostics for edited Python files show no errors

### Required secrets/config to set

- **Render env var**
  - `API_SECRET_KEY=<secure-random-string>`

- **GitHub repo secrets**
  - `RENDER_PIPELINE_TRIGGER_URL` (e.g. `https://<your-render-app>/run-pipeline`)
  - `PIPELINE_TRIGGER_API_KEY` (same value as `API_SECRET_KEY` in Render)

If you want, I can also add a tiny runbook section in your docs explaining exactly how to test the endpoint manually with `curl` and how to verify the GitHub Action logs.

Added a comprehensive runbook and linked it into the docs nav.

### New runbook

- Created `docs/development/pipeline-trigger-runbook.md` with end-to-end operational guidance:
  - architecture and responsibilities
  - Render + GitHub secrets setup
  - endpoint contract (`POST /run-pipeline`, auth, expected responses)
  - GitHub Actions schedule behavior
  - manual verification steps with `curl`
  - overlap/409 behavior validation
  - logging expectations
  - idempotency/restart-safety notes
  - troubleshooting matrix (403/409/500/network)
  - rollback guidance (without reintroducing internal schedulers)

### Documentation updates for consistency

- Updated `docs/development/running-the-project.md` to align with your desired model:
  - local Docker + Airflow remains the local orchestration path
  - removed the instruction to run internal scheduler loops for deployment-like mode
  - pointed production operations to the new runbook

- Updated `docs/data-engineering/etl-pipeline.md`:
  - clarified `src/scraper/scheduler.py` is archived/disabled
  - changed deployment path to external trigger (`POST /run-pipeline` -> `src/jobs/weekly_ingestion.py`)
  - added pointer to the new runbook

- Updated `mkdocs.yml` navigation:
  - added `Pipeline Trigger Runbook` under `Development`

If you want, I can also add a short “Production Checklist” section (copy-paste checklist style) at the top of the runbook for quick go-live verification.