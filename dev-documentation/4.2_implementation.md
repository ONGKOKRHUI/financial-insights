All 22 todos are complete with zero linter errors. Here's a summary of everything that was built:

---

## Phase 4 Implementation Complete

### Backend (`src/backend/`)

- **`requirements.txt`** — added `python-jose[cryptography]`, `passlib[bcrypt]`, `stripe`, `alembic`, `python-multipart`
- **`models.py`** — added `User`, `RefreshToken`, `APIKey` SQLAlchemy models
- **`alembic/`** — full Alembic setup with `env.py`, `script.py.mako`, and migration `001_add_auth_tables.py`
- **`auth/__init__.py`**, **`auth/jwt.py`**, **`auth/password.py`**, **`auth/dependencies.py`** — JWT utilities, bcrypt hashing, SHA-256 API key hashing, and 4 reusable `Depends` functions
- **`routers/auth.py`** — `register`, `login`, `refresh`, `logout` with HttpOnly cookie handling
- **`routers/users.py`** — `/users/me`, `/users/me/api-key`, `/users/me/api-key/rotate`
- **`routers/admin.py`** — paginated user list, role PATCH, account DELETE
- **`routers/webhooks.py`** — Stripe signature verification, `invoice.payment_succeeded` → upgrade user, `customer.subscription.deleted` → downgrade user
- **`routers/search.py`** — gated with `require_api_key_or_session` (paid/admin only)
- **`main.py`** — all new routers registered, CORS updated for credentials, version bumped to 2.0.0

### Frontend (`frontend/src/`)

- **`middleware.ts`** — edge middleware protecting `/dashboard/**`, `/account`, `/admin/**`
- **`stores/authStore.ts`** — Zustand store with `user`, `isHydrating`, `setUser`, `clearUser`
- **`hooks/useAuth.ts`** — `useCurrentUser`, `useLogin`, `useRegister`, `useLogout` TanStack Query hooks
- **`app/api/auth/**`** — BFF proxy routes for register, login, logout, me
- **`app/api/users/api-key/`**, **`app/api/admin/users/`**, **`app/api/stripe/checkout/`** — BFF routes
- **`app/auth/login/page.tsx`**, **`app/auth/register/page.tsx`** — auth pages with one-time password modal
- **`app/dashboard/page.tsx`**, **`app/dashboard/[ticker]/page.tsx`** — paid analytics pages
- **`components/charts/SentimentOverlayChart.tsx`** — AI sentiment overlay (ComposedChart)
- **`components/charts/PeerRadarChart.tsx`** — 5-axis peer comparison radar
- **`components/charts/WaterfallChart.tsx`** — revenue-to-net-income waterfall
- **`app/account/page.tsx`**, **`app/upgrade/page.tsx`**, **`app/admin/dashboard/page.tsx`**
- **`lib/api.ts`** — extended with `api.auth`, `api.user`, `api.stripe` namespaces

### Documentation (`docs/`)

- **Updated:** `api-reference/authentication.md`, `backend/database-schema.md`, `frontend/architecture.md`, `frontend/dashboard.md`, `frontend/state-management.md`
- **New:** `docs/backend/authentication.md`, `docs/backend/rbac.md`, `docs/backend/stripe-integration.md`
- **`mkdocs.yml`** — three new backend pages added to nav