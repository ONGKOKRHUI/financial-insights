Here is a concise repo map focused on scraper → PDF → LLM → DB, mock data, and validation.

---

## Key files (absolute paths)

| Area | Paths |
|------|--------|
| **Scraper orchestration** | `src/scraper/main.py` |
| **Scheduler** | `src/scraper/scheduler.py` (archived; weekly job is `src/jobs/weekly_ingestion.py`) |
| **Company scrapers** | `src/scraper/scrapers/` (`maybank.py`, `cimb.py`, `tnb.py`, `petronas.py`, `maxis.py`, `telekom.py`, `genting.py`, `sunway.py`) |
| **Pipeline graph / CLI** | `src/pipeline/graph.py` |
| **PDF → Markdown** | `src/pipeline/nodes/parser.py` |
| **Routing** | `src/pipeline/nodes/router.py` |
| **Gemini extraction** | `src/pipeline/nodes/quantitative.py`, `qualitative.py` |
| **Merge + Pydantic validation** | `src/pipeline/nodes/merger.py` |
| **Payload schemas** | `src/pipeline/schemas.py` |
| **Dify alternate engine** | `src/pipeline/dify_client.py` |
| **DB UPSERT + `pipeline_runs`** | `src/db/loader.py` |
| **Airflow DAG (PDF ETL)** | `dags/finsight_etl_dag.py` |
| **Airflow DAG (ML features)** | `dags/ml_features_etl_dag.py` |
| **ML pipeline runner** | `src/scraper/ml_pipeline_runner.py` |
| **ML phase modules** | `src/scraper/ml_features/` (`phase_1`…`phase_5`, `investing_com.py`, `i3investor.py`) |
| **ORM models** | `src/backend/models.py` |
| **Seed from mock data** | `src/backend/seed.py` |
| **Backend entry / lifespan** | `src/backend/main.py` |
| **Alembic** | `src/backend/alembic/versions/` (`001` auth, `002` predictive_features) |
| **Docs (pipelines)** | `docs/data-engineering/pdf-parsing.md`, `etl-pipeline.md`, `scraping-system.md`, `ml-features-etl.md` |
| **Dev docs (ML ETL)** | `dev-documentation/6_ml_features_etl.md` |

---

## Current data flow

1. **Scraper** (`main.py`): Playwright + stealth; `COMPANY_CONFIG` drives which modules run and which `(year, quarter)` tuples are missing under `src\scraper\data\raw\{COMPANY}\{COMPANY}_{YEAR}_{Q}.pdf`. Skips existing files (idempotent backfill); `--latest` limits to “current completed quarter.”  
2. **Scheduler** (`scheduler.py`): Runs `main.main(latest_only=True)` on startup and weekly Mondays 09:00; optional commented Sunday full backfill.  
3. **ETL (intended production path)** (`finsight_etl_dag.py`):  
   - `get_unprocessed_pdfs(RAW_DIR)` vs `pipeline_runs.status = 'success'`  
   - For each PDF: `mark_processed(..., processing)` → `run_pipeline(pdf_path)` → XCom payloads → `upsert_report(payload)` → `mark_processed(..., success|error)`  
4. **`run_pipeline`** (`graph.py`): Default **`PIPELINE_ENGINE=langgraph`**: `parse_pdf` → `route_content` → parallel `extract_quantitative` / `extract_qualitative` → `merge_and_validate`. Alternate **`PIPELINE_ENGINE=dify`**: parse only, then Dify workflow.  
5. **Parse**: LlamaCloud `AsyncLlamaCloud` when `LLAMA_CLOUD_API_KEY` is set; otherwise PyMuPDF (`parser.py`). Docs describe `parsing_instruction` for Markdown tables; current `parser.py` snippet in search shows `tier="agentic"` and `expand=["markdown_full"]` — compare with docs if HTML tables appear.  
6. **Metadata**: From filename `{TICKER}_{YEAR}_{PERIOD}.pdf` in `_extract_metadata_from_path` (`parser.py`).  
7. **Persistence**: `loader.py` upserts **five financial tables** + maintains **`pipeline_runs`**. Financial rows keyed by **`(ticker, fiscal_year)`** — same key for all quarters if `fiscal_year` is only the year digit from the filename (see gaps below).

8. **ML Features ETL** (`ml_features_etl_dag.py` / `ml_pipeline_runner.py`): Weekly five-phase pipeline writes **19 populated metrics** (of 21 schema columns) per **`(ticker, fiscal_year, fiscal_quarter)`** into **`predictive_features`**. Sources: yfinance, TradingView sector peers, Investing.com earnings, i3investor KLSE HTML, Malaysia Warrants IV. Sector peer sentiment uses a fast Investing.com peer pass, revenue beat rate first, EPS beat rate fallback, and bounded slow fallback for undersampled sectors. Metrics 19–20 (`guidance_beat_indicator`, `backlog_order_book_yoy_growth_pct`) are schema-only for now. See `dev-documentation/6_ml_features_etl.md`.

---

## Missing or fragile integration points

- **Scraper ↔ pipeline**: No code path in the scraper invokes `run_pipeline` or the loader; ingestion assumes **Airflow (or a manual script)** scans `FINSIGHT_RAW_DIR` (default `...\src\scraper\data\raw`). Local dev without Airflow needs a manual loop or cron.  
- **Quarter vs DB grain**: Loader and ORM use **`fiscal_year` only** for income/balance/cash/kpi/qualitative. Filenames encode quarter (`Q1`…`Q4`), but merger uses **`fiscal_year` from the middle segment** (`parser.py`). Multiple quarters in the same calendar year likely **overwrite** the same `(ticker, fiscal_year)` row unless extraction maps quarters differently — worth validating against product intent.  
- **`companies` vs loader**: `upsert_report` writes child tables with FK to `companies.ticker`. **No loader step inserts `Company`**; tickers must already exist (e.g. from seed) or inserts can fail on FK.  
- **`mock_data.py`**: `seed.py` imports `from data.mock_data import ...` (expected under `src/backend/` working dir). **No `mock_data.py` showed up in workspace search** — it may be gitignored, not committed, or missing clone artifact; backend seed would fail until present.  
- **Alembic vs core schema**: Only **`users`, `refresh_tokens`, `api_keys`** are migrated. **`companies` + financial tables** are implied to come from **`Base.metadata.create_all`** at startup (`main.py` pattern); align deployments accordingly.  
- **`pipeline_runs` paths**: Tracking uses absolute paths from `get_unprocessed_pdfs`; consistency across machines/job runners matters.

---

## Company list sources

- **Scraper**: Hardcoded **`COMPANY_CONFIG`** in `src/scraper/main.py` (Maybank, Sunway, Genting, Telekom, Maxis, Petronas, TNB, CIMB — each module's `COMPANY` constant and date/quarter ranges).  
- **API / dashboard narrative**: Backend describes eight Malaysian blue-chips; **seed data** is meant to populate **`companies`** when empty (`seed.py`).  
- **Pipeline**: **Ticker** comes from **PDF basename**, not a central registry — must match DB tickers used in seed/FKs.

---

## Database models / tables

From `src/backend/models.py` and `loader.py`:

- **Financial / domain**: `companies`, `kpi_summaries`, `income_statements`, `balance_sheets`, `cash_flows`, `qualitative_insights` (all statement-like tables have unique `(ticker, fiscal_year)`).  
- **ML training features**: `predictive_features` (unique `(ticker, fiscal_year, fiscal_quarter)` — 21 metric columns). Migration `002_add_predictive_features.py`.  
- **Auth (Phase 4)**: `users`, `refresh_tokens`, `api_keys`.  
- **ETL tracking (loader DDL only)**: `pipeline_runs` (`pdf_path` unique, `status`, `ticker`, `fiscal_year`, `run_at`, `error_msg`).

---

## `mock_data` usage

- **`seed_if_empty`** (`seed.py`): If `Company` table has no rows, inserts companies + KPI + statements + cash flows + qualitative from **`mock_data`** structures (`COMPANIES`, `KPI_SUMMARIES`, etc.).  
- **Tests**: `src\backend\tests\conftest.py` imports from **`data.mock_data`** for SQLite fixtures (per grep/docs).

---

## Ground truth / validation structure

- **No dedicated `ground_truth/` dataset** surfaced in search.  
- **Validation**: **Pydantic** `FinancialReportPayload` and sub-schemas in **`merge_and_validate`** (`merger.py`); partial branch validation with degraded payloads on failure.  
- **Artifacts**: `FILE_SUMMARY.md` references **`results_sunway_2021_q1.json`** for manual validation — file not verified present in listing.  
- **Automated tests**: Tiered **`tests/test_pipeline.py`** (imports/smoke vs `--integration` with real PDF + DB); **`tests/test_phase2_extraction.py`**, **`tests/debug_extraction.py`**, **`tests/test_llama_parse.py`** (LlamaParse smoke with hardcoded PDF path); **`tests/test_phase3_api_integration.py`**, **`tests/test_phase4_*`**, **`tests/test_bugs.py`**; **`src/backend/tests/test_api.py`**; ML features: **`tests/test_ml_features_types.py`**, **`tests/test_investing_com_parser.py`**, **`tests/test_i3investor_scrapers.py`**, **`tests/test_phase_5_bursa_pdf.py`**, **`src/backend/tests/test_predictive_features.py`**.

---

## Likely tests / commands for validation

- **Smoke (no keys)**:  
  `python tests/test_pipeline.py --smoke`  
- **Full pipeline script tests**:  
  `python tests/test_pipeline.py`  
  `python tests/test_pipeline.py --integration --pdf <path/to/report.pdf>`  
- **Single PDF CLI**:  
  `PYTHONPATH=src python -m pipeline.graph --pdf <path>`  
- **Pytest API / phases**:  
  `pytest tests/test_phase3_api_integration.py -v`  
  `pytest tests/test_phase4_full_stack.py -v`  
  `pytest src/backend/tests/test_api.py -v`  
- **LlamaParse**:  
  `python tests/test_llama_parse.py` (needs `LLAMA_CLOUD_API_KEY`; PDF path inside file may need updating).  
- **ML feature pipeline dry run**:  
  `cd src/scraper && python ml_pipeline_runner.py --tickers MAYBANK --dry-run`  
- **ML feature pipeline tests**:  
  `pytest tests/test_ml_features_types.py tests/test_investing_com_parser.py tests/test_i3investor_scrapers.py tests/test_phase_5_bursa_pdf.py -v`  
  `pytest src/backend/tests/test_predictive_features.py -v`  
- **Backend CI parity**:  
  From deploy workflow: `pip install -r src/backend/requirements.txt`, then `python -m compileall .` and `python -c "from main import app"` under `src/backend`.  

---

**Summary:** The scraper only fills `data/raw`; the Airflow DAG (`finsight_etl_dag.py`) connects PDFs to `run_pipeline` and `upsert_report`. LlamaParse + Gemini live under `src/pipeline/nodes/` with validation in `merger.py` / `schemas.py`. Mock seeding is wired via `seed.py` but **`mock_data.py` was not found** in this workspace snapshot. Validation is schema-based and script/pytest tiered — **no formal ground-truth suite** beyond docs and optional JSON samples.